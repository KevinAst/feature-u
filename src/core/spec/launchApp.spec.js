//import {op}           from '../launchApp';  // module under test INTERNAL 
//import createAspect$  from './createAspect$';

//??????????????????????????
describe('more', () => {
  test('TODO ??', () => {
    expect(1)
      .toEqual(1);
  });
});

// ?? TODO: describe('check order of life cycle hooks (both Features and Aspects', () => {

// ?? TODO: launchApp.js NEEDS to isolate it's parameter violations so it can be tested in isolation
